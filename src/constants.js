// Creating Db Name
const DB_NAME = "TEST-DB";

//Cookies

const COOKIES_OPTIONS = {
  httpOnly: true,
  secure: true,
  sameSite: "Strict",
};

// Exporting Db Name
export { DB_NAME, COOKIES_OPTIONS };
